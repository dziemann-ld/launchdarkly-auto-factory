{
  "name": "@auto-factory/phase1-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless Phase 1 front end: the full AutoFactory chain (shared core) against a local working tree, driven from a terminal or a Claude Code session.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "autofactory": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@auto-factory/shared": "*"
  }
}
